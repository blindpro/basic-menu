# basic menu.

Turns the menu object from a menu with indexes into a callback system.

## how to use.

Include it in your project, include the basic menu class.

Make each menu item a function, then add it using the add_item method.

Add other menus using the add_connecter_menu method, and point it to another basic_menu.